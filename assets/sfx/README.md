# Sound Effects

Place a short, subtle pop or whoosh sound effect here and set `SFX_POP_PATH` in `.env`.

Default expected file:

```text
assets/sfx/pop.mp3
```

Keep it very short, around 50-180 ms, and normalize it quietly. ClipMind applies
`SFX_VOLUME` during compositing so it does not overpower speech.
